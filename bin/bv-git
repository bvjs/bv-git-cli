#!/usr/bin/env node

const program = require('commander')
const packageConfig = require('../package')

program
  .version(packageConfig.version)
  .usage('<command> [options]')
  .command('init', 'create new config')
  .command('run', 'start downloading')
  .parse(process.argv)
